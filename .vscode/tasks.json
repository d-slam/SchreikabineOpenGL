{
    "tasks": [
        {
            "type": "shell",
            "label": "Build SchreikabineOpenGL (CMake Debug)",
            "command": "cmake",
            "args": [
                "--build",
                "--preset",
                "debug"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build the SchreikabineOpenGL JUCE CMake target in Debug mode."
        }
    ],
    "version": "2.0.0"
}